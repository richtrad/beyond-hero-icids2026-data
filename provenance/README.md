# Provenance

`originals.json` lists source basenames, original SHA-256, archived path, archived SHA-256 and transformations. The local originals were read from the author's `Documents/DataSteam`, `Downloads` and `Documents/Věda/grafitctu.github.io/archetype-validator-git` folders.

The interface came from local Git revision `8d303343d0e57be7ebfd4fc48066a60dcea4ca17`, last affecting that directory on 1 July 2026. No changes in the interface directory were reported by Git when it was inspected. This establishes the recovered revision, not which revision each participant saw.

The demo omits the original `config.js` and `google-apps-script.js`. It substitutes a configuration with no collection endpoint. Personal participant response files are not included.

No original interactive Codex annotation task or complete ChatGPT transcript has yet been recovered. Prompts found in local scripts are identified as such, and unevaluated template expressions are labelled in `prompts/index.json`.

The literature comparison was checked against the author-uploaded full text of Freire-Sánchez and Vidal-Mestre (2024), specifically Methodology and Tables 2–3. Bibliographic metadata was cross-checked against institutional repository records. See `comparison/sources.bib`.
